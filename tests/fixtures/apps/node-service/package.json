{
  "name": "node-service-fixture",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp src/server.js dist/server.js",
    "start": "node dist/server.js",
    "test": "node --test"
  }
}
